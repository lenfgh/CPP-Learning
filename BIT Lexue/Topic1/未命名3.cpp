#include <iostream>  
#include <cmath> // ?????,??????????  
  
using namespace std;  
  
int main() {  
    int n;  
    cin >> n; // ??n??,?????????  
  
    // ?????????  
    int size = pow(2, 2 + n);  
    int a[size][size];  
  
    // ????????  
    for (int i = 0; i < size; ++i) {  
        for (int j = 0; j < size; ++j) {  
            a[i][j] = ' ';  
        }  
    }  
  
    // ????????,????????"/",????  
    // ?????,?????????????  
    int centerX = size / 2;  
    int centerY = size / 2;  
    a[centerX][centerY] = '/';  
  
    // ????  
    for (int i = 0; i < size; ++i) {  
        for (int j = 0; j < size; ++j) {  
            cout << a[i][j];  
        }  
        cout << endl;  
    }  
  
    return 0;  
}
